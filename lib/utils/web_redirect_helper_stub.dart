// Stub para plataformas no-web
// Este archivo se usa cuando no se compila para web

void redirectToUrlWeb(String url) {
  // No hacer nada en plataformas no-web
  // Esta función nunca debería llamarse en móvil porque
  // se verifica kIsWeb antes de llamarla
}
